meta {
  name: Delete User By Id
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/users/{{id_user}}
  body: none
  auth: inherit
}

vars:pre-request {
  id_user: 
}

assert {
  res.status: eq 204
}

settings {
  encodeUrl: true
  timeout: 0
}
