meta {
  name: Create Wallet
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/wallets
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Nubank",
    "description": "Conta Principal",
    "userId": "d440e507-eed8-4810-8d27-097abb1534ea"
  }
}

assert {
  res.status: eq 201
  res.body.id: isNumber
  res.body.name: isString
  res.body.userId: isString
  res.body.userName: isString
}

settings {
  encodeUrl: true
  timeout: 0
}
