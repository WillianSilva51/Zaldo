meta {
  name: Find Transaction By Id
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/transactions/{{transactionId}}
  body: none
  auth: inherit
}

vars:pre-request {
  transactionId: 12
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
